# "Big" Example

The purpose of this example is to demonstrate how to communicate through the Serial port to a computer. Although most of the code is just there to render to the screen, the `read_new_dots` python function is the most important part.

The arduino code is similar to the 'Sweep' default Arduino example combined with the `Distance.ino` small example in this repository.
